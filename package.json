{
  "name": "desktop-website",
  "description": "Dat Desktop project site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "start": "bankai start --open",
    "build": "bankai build -O",
    "test": "standard && npm run deps"
  },
  "browserify": {
    "transform": [
      "brfs"
    ]
  },
  "dependencies": {
    "brfs": "^1.4.3",
    "choo": "^4.0.3",
    "choo-log": "^3.0.0",
    "sheetify": "^6.0.1",
    "tachyons": "^4.6.1"
  },
  "devDependencies": {
    "bankai": "^5.2.1",
    "dependency-check": "^2.7.0",
    "standard": "^8.6.0"
  }
}
